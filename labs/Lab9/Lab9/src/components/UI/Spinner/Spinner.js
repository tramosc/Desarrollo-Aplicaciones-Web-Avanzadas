import React, { Fragment } from 'react';

import './Spinner.css';

const Spinner = () => {
    return (
        <Fragment>
            <div class="lds-hourglass"></div>
        </Fragment>
    );
};

export default Spinner;